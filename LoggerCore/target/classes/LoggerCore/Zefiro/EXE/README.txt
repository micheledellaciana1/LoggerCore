Zefiro runs directly with java JRE installed on the pc.
To run correctly, Zefiro needs a config.txt file situated in the same location where it is the program. In config.txt are defined the variables that configure the GUI and the device.
To run the program in simulation mode, set Simulate:true.
