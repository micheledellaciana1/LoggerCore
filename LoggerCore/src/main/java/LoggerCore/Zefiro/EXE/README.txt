Zefiro runs directly with java JRE installed on the pc.
To runs correctly zefiro need a config.txt file in the same directory where it located Zefiro. In that files are defined variables that configure the GUI and if connected the device.
To run the program in simulation mode, set Simulate:true.
